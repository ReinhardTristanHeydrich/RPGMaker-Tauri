name: Create Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - name: Add Ubuntu Jammy repository
        run: |
          echo "Types: deb" | sudo tee /etc/apt/sources.list.d/ubuntu-jammy.sources
          echo "URIs: http://gb.archive.ubuntu.com/ubuntu" | sudo tee -a /etc/apt/sources.list.d/ubuntu-jammy.sources
          echo "Suites: jammy" | sudo tee -a /etc/apt/sources.list.d/ubuntu-jammy.sources
          echo "Components: main" | sudo tee -a /etc/apt/sources.list.d/ubuntu-jammy.sources
          echo "Signed-By: /usr/share/keyrings/ubuntu-archive-keyring.gpg" | sudo tee -a /etc/apt/sources.list.d/ubuntu-jammy.sources
          sudo apt-get update

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.0-dev \
            build-essential \
            curl \
            wget \
            libssl-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev

      - name: Install app dependencies
        run: npm install

      - name: Build Tauri app
        run: npm run tauri build

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          draft: false
          prerelease: false

      - name: Upload Release Assets
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: src-tauri/target/release/bundle/deb/*.deb
          asset_name: ${{ github.event.repository.name }}_${{ github.ref_name }}_amd64.deb
          asset_content_type: application/vnd.debian.binary-package